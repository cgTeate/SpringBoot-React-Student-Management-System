


version: "3.8"
services:
  backend:
    image: "cgteate/springboot-react-fullstack:16.3.2023.21.9.0"
    ports:
      - "80:8080"
    restart: "always"
    environment:
      SPRING_PROFILES_ACTIVE: dev
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}